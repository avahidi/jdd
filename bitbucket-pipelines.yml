# image: java
image: maven:3.6.1
pipelines:
  default:
    - step:
        script:
          - apt-get update && apt-get install -y graphviz
          - mvn clean
          - mvn compile
          - mvn test
