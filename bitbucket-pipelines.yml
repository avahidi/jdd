# image: java
image: maven:3.6.1
pipelines:
  default:
    - step:
        script:
          - apt update
          - apt install -y graphviz maven
          - mvn clean
          - mvn package
          - mvn test
